// import * as Select from "@radix-ui/react-select";
// import { forwardRef, memo } from "react";

// const FrequencySelect: React.FC = forwardRef<HTMLDivElement>(({}, ref) => (
//   <div
//     ref={ref}
//     className={cn("flex flex-col flex-1 gap-1", {
//       //   "w-full": frequency === "monthly",
//     })}
//   >
//     {/* <Label htmlFor="frequency-select">{data.frequency}</Label>
//     <Select onValueChange={(value: Frequency) => setFrequency(value)}>
//       <SelectTrigger id="frequency-select">
//         {data.frequencies[frequency]}
//       </SelectTrigger>
//       <SelectContent>
//         <SelectGroup>
//           {Object.entries(data.frequencies).map(([key, value]) => (
//             <SelectItem key={value} value={key}>
//               {value}
//             </SelectItem>
//           ))}
//         </SelectGroup>
//       </SelectContent>
//     </Select> */}
//   </div>
// ));

// FrequencySelect.displayName = "FrequencySelect";

// export default memo(FrequencySelect);
